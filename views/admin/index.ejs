<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= (typeof product !== 'undefined' && product) ? (product.name_zh || product.name_en || 'Product') : ((config && config.site_name) || 'AutoParts B2B') %> | Auto Parts</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --main: #0A2F5F; --blue: #1E6BD9; --accent: #F59E0B; --light: #F3F4F6;
      --text: #1F2937; --muted: #6B7280; --white: #fff; --r: 12px;
    }
    * { margin:0; padding:0; box-sizing:border-box; font-family:'Inter', system-ui, sans-serif; }
    body { background: #F1F5F9; color: var(--text); line-height:1.6; min-height:100vh; }
    a { text-decoration:none; color:inherit; }
    img { max-width:100%; display:block; }

    /* 整页背景：柔和遮罩，避免刺眼 */
    body.page-bg-full { background-size:cover; background-position:center; background-attachment:fixed; }
    body.page-bg-full::before {
      content:''; position:fixed; inset:0; z-index:-1; pointer-events:none;
      background: linear-gradient(180deg, rgba(248,250,252,.92), rgba(241,245,249,.94));
    }

    /* Header */
    .header {
      position:sticky; top:0; z-index:100; background:var(--main); color:#fff;
      box-shadow:0 2px 12px rgba(0,0,0,.15);
    }
    .header-inner {
      max-width:1200px; margin:0 auto; padding:14px 20px;
      display:flex; align-items:center; justify-content:space-between; gap:16px; flex-wrap:wrap;
    }
    .logo { font-size:18px; font-weight:700; letter-spacing:-.02em; }
    .nav { display:flex; align-items:center; gap:16px; flex-wrap:wrap; }
    .nav a { color:rgba(255,255,255,.9); font-size:14px; font-weight:500; }
    .nav a:hover { color:var(--accent); }
    .lang-cur { display:flex; align-items:center; gap:8px; }
    .lang-cur button {
      background:transparent; border:1px solid rgba(255,255,255,.4); color:#fff;
      padding:5px 12px; border-radius:6px; font-size:12px; cursor:pointer; font-weight:500;
    }
    .lang-cur button.active { background:var(--accent); border-color:var(--accent); color:#111; font-weight:700; }
    /* 货币下拉：深色字 + 白底，展开可见 */
    #currencySelect {
      appearance:auto; -webkit-appearance:menulist;
      background:#fff !important; color:#111 !important;
      border:1px solid #D1D5DB; border-radius:6px;
      padding:5px 8px; font-size:12px; font-weight:600; cursor:pointer; min-width:72px;
    }
    #currencySelect option { background:#fff; color:#111; }

    /* Hero：更干净的遮罩，文字更清晰 */
    .hero {
      padding:64px 20px; text-align:center; position:relative; overflow:hidden;
      background: linear-gradient(135deg, #0A2F5F 0%, #1E6BD9 55%, #3B82F6 100%);
      color:#fff;
    }
    .hero::before {
      content:''; position:absolute; inset:0; z-index:0;
      <% if (config && config.hero_bg) { %>
      background-image: url('<%= config.hero_bg %>');
      background-size:cover; background-position:center;
      opacity: <%= Math.min(0.35, parseFloat(config.hero_bg_opacity||0.3) || 0.3) %>;
      <% } else { %>
      opacity:0;
      <% } %>
    }
    .hero::after {
      content:''; position:absolute; inset:0; z-index:0;
      background: linear-gradient(135deg, rgba(10,47,95,.82), rgba(30,107,217,.75));
    }
    .hero > * { position:relative; z-index:1; }
    .hero h1 { font-size:clamp(1.55rem,4vw,2.35rem); margin-bottom:12px; font-weight:700; text-shadow:0 1px 2px rgba(0,0,0,.2); }
    .hero p { color:rgba(255,255,255,.9); max-width:640px; margin:0 auto 24px; font-size:15px; }
    .btn {
      display:inline-flex; align-items:center; justify-content:center; gap:6px;
      padding:11px 22px; border-radius:8px; font-size:14px; font-weight:600; border:none; cursor:pointer;
      transition: box-shadow .15s, background .15s; /* 无缩放抖动 */
    }
    .btn-main { background:#fff; color:var(--main); box-shadow:0 4px 14px rgba(0,0,0,.15); }
    .btn-main:hover { box-shadow:0 6px 18px rgba(0,0,0,.2); }
    .btn-accent { background:var(--accent); color:#111; }
    .btn-outline { background:transparent; color:#fff; border:1.5px solid rgba(255,255,255,.6); }

    /* Factory */
    .section { padding:48px 20px; }
    .section-title { text-align:center; font-size:clamp(1.25rem,3vw,1.6rem); font-weight:700; color:var(--main); margin-bottom:6px; }
    .section-sub { text-align:center; color:var(--muted); font-size:14px; margin-bottom:28px; }
    .factory-grid {
      max-width:1100px; margin:0 auto;
      display:grid; grid-template-columns:1.1fr 1fr; gap:28px; align-items:center;
    }
    .factory-slider {
      position:relative; border-radius:14px; overflow:hidden; aspect-ratio:16/10;
      background:#E2EBF5; box-shadow:0 8px 28px rgba(10,47,95,.08);
    }
    .factory-slider .slide {
      position:absolute; inset:0; opacity:0; background-size:cover; background-position:center;
      transition: opacity .7s ease;
    }
    .factory-slider .slide.active { opacity:1; }
    .slider-dots {
      position:absolute; bottom:12px; left:0; right:0; display:flex; justify-content:center; gap:6px; z-index:2;
    }
    .slider-dots span {
      width:8px; height:8px; border-radius:50%; background:rgba(255,255,255,.5); cursor:pointer;
    }
    .slider-dots span.active { background:#fff; transform:scale(1.2); }
    .factory-info h3 { font-size:1.2rem; color:var(--main); margin-bottom:10px; }
    .factory-info p { color:var(--muted); font-size:14px; margin-bottom:14px; }
    .adv-list { list-style:none; }
    .adv-list li { display:flex; gap:10px; align-items:flex-start; margin-bottom:8px; font-size:14px; }
    .adv-list .icon {
      width:22px; height:22px; border-radius:50%; background:linear-gradient(135deg,#1E6BD9,#0A2F5F);
      color:#fff; font-size:11px; display:flex; align-items:center; justify-content:center; flex-shrink:0;
    }

    /* Products：底图用轻遮罩，内容可读 */
    .products-section {
      padding:48px 16px 64px; position:relative;
      background: #EEF2F7;
    }
    .products-section.has-bg::before {
      content:''; position:absolute; inset:0; z-index:0;
      background-size:cover; background-position:center;
      opacity: 0.18;
      pointer-events:none;
    }
    .products-section > * { position:relative; z-index:1; }
    .products-layout {
      max-width:1180px; margin:0 auto;
      display:grid; grid-template-columns:200px 1fr; gap:20px; align-items:start;
    }
    .sidebar-cat {
      position:sticky; top:74px; background:#fff; border-radius:var(--r); padding:16px;
      box-shadow:0 2px 10px rgba(0,0,0,.05); border:1px solid #E5E7EB;
    }
    .sidebar-cat h4 { font-size:12px; color:var(--muted); margin-bottom:10px; letter-spacing:.04em; }
    .cat-list a {
      display:block; padding:9px 12px; border-radius:8px; font-size:13px; color:var(--text);
      margin-bottom:4px; transition: background .15s, color .15s;
    }
    .cat-list a:hover { background:#EBF2FB; color:var(--blue); }
    .cat-list a.active { background:var(--blue); color:#fff; font-weight:600; }

    .toolbar { display:flex; gap:10px; flex-wrap:wrap; margin-bottom:16px; align-items:center; }
    .toolbar input, .toolbar select {
      padding:9px 12px; border:1px solid #D1D5DB; border-radius:8px; font-size:13px; outline:none; background:#fff; color:#111;
    }
    .toolbar input:focus { border-color:var(--blue); }
    .product-grid {
      display:grid; grid-template-columns:repeat(auto-fill,minmax(220px,1fr)); gap:16px;
    }
    /* 卡片：取消上移抖动，仅轻微阴影 */
    .card {
      background:#fff; border-radius:var(--r); overflow:hidden;
      box-shadow:0 1px 4px rgba(0,0,0,.06); border:1px solid #E5E7EB;
      transition: box-shadow .2s, border-color .2s;
    }
    .card:hover {
      box-shadow:0 6px 20px rgba(10,47,95,.1);
      border-color:#C7D2FE;
    }
    .card:active { box-shadow:0 1px 4px rgba(0,0,0,.08); }
    .card-img { aspect-ratio:1; background:#F1F5F9; overflow:hidden; }
    .card-img img { width:100%; height:100%; object-fit:cover; transition:none; }
    .card-body { padding:14px; }
    .card-body h3 { font-size:14px; font-weight:600; color:var(--main); margin-bottom:6px; line-height:1.35; }
    .card-meta { font-size:12px; color:var(--muted); margin-bottom:8px; }
    .price { font-size:16px; font-weight:700; color:var(--blue); }
    .price .cur-sym { font-size:12px; margin-right:2px; }

    /* Detail */
    .detail-wrap { max-width:1000px; margin:0 auto; padding:32px 20px 60px; }
    .detail-grid { display:grid; grid-template-columns:1fr 1fr; gap:32px; align-items:start; }
    .detail-img { border-radius:var(--r); overflow:hidden; background:#F1F5F9; aspect-ratio:1; border:1px solid #E5E7EB; }
    .detail-img img { width:100%; height:100%; object-fit:cover; }
    .detail-info h1 { font-size:1.5rem; color:var(--main); margin-bottom:12px; }
    .detail-info .meta { background:#F8FAFC; padding:12px 14px; border-radius:8px; margin:14px 0; font-size:14px; border:1px solid #E5E7EB; }
    .detail-info .meta p { margin-bottom:4px; }
    .spec-list { list-style:none; margin:16px 0; }
    .spec-list li { padding:8px 0; border-bottom:1px solid #E5E7EB; font-size:14px; }

    footer { background:var(--main); color:#CBD5E1; padding:40px 20px 24px; margin-top:40px; }
    .foot-inner { max-width:1100px; margin:0 auto; display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:28px; }
    .foot-col h4 { color:var(--accent); margin-bottom:12px; font-size:15px; }
    .foot-col p, .foot-col a { font-size:13px; line-height:1.8; color:#94A3B8; }
    .copyright { text-align:center; margin-top:28px; padding-top:16px; border-top:1px solid rgba(255,255,255,.1); font-size:12px; color:#64748B; }

    .wa-float {
      position:fixed; bottom:24px; right:24px; z-index:90;
      background:#25D366; color:#fff; padding:12px 18px; border-radius:50px;
      box-shadow:0 4px 16px rgba(37,211,102,.4); font-weight:600; font-size:14px;
      display:flex; align-items:center; gap:8px;
    }
    .empty { text-align:center; padding:48px 20px; color:var(--muted); }

    /* 语言：用 class 切换显隐 */
    .i18n-en { display:none !important; }
    html[lang="en"] .i18n-zh { display:none !important; }
    html[lang="en"] .i18n-en { display:inline !important; }
    html[lang="en"] .i18n-en-block { display:block !important; }
    .i18n-en-block { display:none !important; }
    html[lang="zh-CN"] .i18n-en-block, html[lang="zh"] .i18n-en-block { display:none !important; }

    @media (max-width:900px) {
      .factory-grid, .detail-grid, .products-layout { grid-template-columns:1fr !important; }
      .sidebar-cat { position:static; }
      .cat-list { display:flex; flex-wrap:wrap; gap:6px; }
      .cat-list a { margin:0; }
      .product-grid { grid-template-columns:repeat(auto-fill,minmax(150px,1fr)); }
    }

    /* 回顶按钮 */
    .scroll-top {
      position:fixed; bottom:90px; right:24px; z-index:89;
      width:44px; height:44px; border-radius:50%; border:none; cursor:pointer;
      background:var(--main); color:#fff; font-size:20px; box-shadow:0 4px 14px rgba(0,0,0,.2);
      display:none; align-items:center; justify-content:center;
      transition: opacity .2s, background .2s;
    }
    .scroll-top.show { display:flex; }
    .scroll-top:hover { background:var(--blue); }
    /* 手机返回 */
    .mobile-back {
      display:none; position:fixed; top:12px; left:12px; z-index:110;
      width:40px; height:40px; border-radius:50%; border:none; cursor:pointer;
      background:rgba(10,47,95,.9); color:#fff; font-size:18px;
      align-items:center; justify-content:center; box-shadow:0 2px 10px rgba(0,0,0,.25);
    }
    @media (max-width:768px) {
      .mobile-back { display:flex; }
      .header-inner { padding-left:56px; }
    }
    /* 社交图标 */
    .social-bar {
      display:flex; flex-wrap:wrap; gap:14px; justify-content:center; margin-top:16px;
    }
    .social-item {
      display:flex; flex-direction:column; align-items:center; gap:6px;
      text-decoration:none; color:#94A3B8; font-size:12px; min-width:64px;
    }
    .social-item img {
      width:40px; height:40px; border-radius:50%; object-fit:cover;
      border:2px solid rgba(255,255,255,.2); background:#fff;
    }
    .social-item:hover { color:var(--accent); }
    .social-item:hover img { border-color:var(--accent); }


    /* 询盘区 */
    .inquiry-section {
      padding: 56px 20px;
      background: linear-gradient(180deg, #F0F5FB 0%, #E8EEF6 100%);
      position: relative;
      overflow: hidden;
    }
    .inquiry-section.has-bg::before {
      content:''; position:absolute; inset:0; background-size:cover; background-position:center;
      opacity:0.12; pointer-events:none;
    }
    .inquiry-section > * { position:relative; z-index:1; }
    .inquiry-cards {
      max-width: 1000px; margin: 28px auto 0;
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px;
    }
    .inquiry-card {
      background:#fff; border-radius:14px; padding:24px 20px; text-align:center;
      box-shadow:0 4px 16px rgba(10,47,95,.06); border:1px solid #E5E7EB;
      transition: box-shadow .2s;
    }
    .inquiry-card:hover { box-shadow:0 8px 24px rgba(10,47,95,.1); }
    .inquiry-card .ic {
      width:48px; height:48px; border-radius:12px; margin:0 auto 14px;
      display:flex; align-items:center; justify-content:center; font-size:22px;
      background:linear-gradient(135deg,#1E6BD9,#0A2F5F); color:#fff;
    }
    .inquiry-card h4 { font-size:15px; color:var(--main); margin-bottom:6px; font-weight:600; }
    .inquiry-card p { font-size:13px; color:var(--muted); word-break:break-all; }
    .inquiry-cta { text-align:center; margin-top:28px; }
    .inquiry-cta .btn { margin:6px; min-width:140px; }
    /* 页脚 */
    footer { background: #071e3d; color:#CBD5E1; padding:48px 20px 24px; margin-top:0; }
    .foot-inner { max-width:1100px; margin:0 auto; display:grid; grid-template-columns:1.4fr 1fr 1fr; gap:36px; }
    .foot-col h4 { color:#F59E0B; margin-bottom:14px; font-size:15px; font-weight:600; letter-spacing:.02em; }
    .foot-col p, .foot-col a { font-size:13px; line-height:1.9; color:#94A3B8; display:block; }
    .foot-col a:hover { color:#F59E0B; }
    .foot-brand { color:#F8FAFC !important; font-weight:600; font-size:15px !important; margin-bottom:8px !important; }
    .foot-social-wrap {
      max-width:1100px; margin:32px auto 0; padding-top:24px;
      border-top:1px solid rgba(255,255,255,.1); text-align:center;
    }
    .foot-social-wrap .lab { font-size:12px; color:#64748B; margin-bottom:14px; text-transform:uppercase; letter-spacing:.06em; }
    .social-bar { display:flex; flex-wrap:wrap; gap:16px; justify-content:center; }
    .social-item {
      display:flex; flex-direction:column; align-items:center; gap:6px;
      text-decoration:none; color:#94A3B8; font-size:12px; min-width:60px;
    }
    .social-item img {
      width:44px; height:44px; border-radius:50%; object-fit:cover;
      border:2px solid rgba(255,255,255,.15); background:#fff;
      transition: border-color .2s, transform .2s;
    }
    .social-item:hover { color:#F59E0B; }
    .social-item:hover img { border-color:#F59E0B; transform:translateY(-2px); }
    .copyright { text-align:center; margin-top:20px; padding-top:16px; border-top:1px solid rgba(255,255,255,.08); font-size:12px; color:#64748B; }
    @media (max-width:800px) {
      .inquiry-cards { grid-template-columns:1fr; max-width:400px; }
      .foot-inner { grid-template-columns:1fr; gap:24px; text-align:center; }
    }

  </style>
</head>
<body class="<%= (config && config.page_bg_full==='1' && config.page_bg) ? 'page-bg-full' : '' %>"
  <% if (config && config.page_bg_full==='1' && config.page_bg) { %>style="background-image:url('<%= config.page_bg %>')"<% } %>>

  <button type="button" class="mobile-back" id="mobileBack" aria-label="Back">←</button>
  <header class="header">
    <div class="header-inner">
      <a href="/" class="logo"><%= (config && config.site_name) || 'AutoParts B2B' %></a>
      <nav class="nav">
        <a href="/"><span class="i18n-zh">首页</span><span class="i18n-en">Home</span></a>
        <a href="/#products"><span class="i18n-zh">产品</span><span class="i18n-en">Products</span></a>
        <a href="/#factory"><span class="i18n-zh">工厂</span><span class="i18n-en">Factory</span></a>
        <a href="/#inquiry"><span class="i18n-zh">询盘</span><span class="i18n-en">Inquiry</span></a>
        <div class="lang-cur">
          <button type="button" data-lang="zh" class="lang-btn active">中文</button>
          <button type="button" data-lang="en" class="lang-btn">EN</button>
          <select id="currencySelect" aria-label="Currency">
            <option value="HKD">HKD</option>
            <option value="USD">USD</option>
            <option value="CNY">CNY</option>
            <option value="KRW">KRW</option>
            <option value="JPY">JPY</option>
          </select>
        </div>
      </nav>
    </div>
  </header>

  <% if (typeof page === 'undefined' || page === 'list') { %>
  <section class="hero">
    <h1>
      <span class="i18n-zh"><%= (config && config.company_name) || '汽配通 · 工业自动化配件' %></span>
      <span class="i18n-en"><%= (config && config.company_name) || 'Auto Parts B2B' %></span>
    </h1>
    <p>
      <span class="i18n-zh">一站式汽车零部件 B2B 采购平台 · OEM / ODM · 全球发货</span>
      <span class="i18n-en">One-stop auto parts B2B platform · OEM / ODM · Global shipping</span>
    </p>
    <div style="display:flex;gap:12px;justify-content:center;flex-wrap:wrap;">
      <a href="#products" class="btn btn-main"><span class="i18n-zh">浏览产品</span><span class="i18n-en">Browse Products</span></a>
      <a href="#inquiry" class="btn btn-accent"><span class="i18n-zh">立即询盘</span><span class="i18n-en">Inquiry Now</span></a>
    </div>
  </section>

  <section class="section" id="factory" style="background:#fff;">
    <h2 class="section-title">
      <span class="i18n-zh"><%= (factory && factory.title_zh) || '现代化生产基地' %></span>
      <span class="i18n-en"><%= (factory && factory.title_en) || 'Modern Manufacturing Base' %></span>
    </h2>
    <p class="section-sub">
      <span class="i18n-zh"><%= (factory && factory.title_en) || '' %></span>
      <span class="i18n-en"><%= (factory && factory.title_zh) || '' %></span>
    </p>
    <div class="factory-grid">
      <div class="factory-slider" id="factorySlider">
        <% 
          var fImgs = [];
          if (factory && factory.images) {
            fImgs = String(factory.images).split(',').map(function(s){return s.trim();}).filter(Boolean);
          }
          if (!fImgs.length) fImgs = ['https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?w=800&q=80'];
          fImgs.forEach(function(src, i){ 
        %>
          <div class="slide <%= i===0?'active':'' %>" style="background-image:url('<%= src %>')"></div>
        <% }); %>
        <div class="slider-dots">
          <% fImgs.forEach(function(_, i){ %><span data-index="<%= i %>" class="<%= i===0?'active':'' %>"></span><% }); %>
        </div>
      </div>
      <div class="factory-info">
        <h3>
          <span class="i18n-zh"><%= (factory && factory.title_zh) || '实力铸就品质' %></span>
          <span class="i18n-en"><%= (factory && factory.title_en) || 'Quality Built on Strength' %></span>
        </h3>
        <p>
          <span class="i18n-zh"><%= (factory && factory.intro_zh) || '专业汽配制造，严格品控，服务全球客户。' %></span>
          <span class="i18n-en"><%= (factory && factory.intro_en) || 'Professional auto parts manufacturing with strict QC for global customers.' %></span>
        </p>
        <ul class="adv-list">
          <% 
            var advZh = (factory && factory.advantages_zh) ? String(factory.advantages_zh).split('|') : ['ISO认证','快速交期','OEM/ODM'];
            var advEn = (factory && factory.advantages_en) ? String(factory.advantages_en).split('|') : advZh;
            for (var ai=0; ai<advZh.length; ai++) {
              if (!String(advZh[ai]).trim()) continue;
          %>
            <li>
              <span class="icon">✓</span>
              <span>
                <span class="i18n-zh"><%= String(advZh[ai]).trim() %></span>
                <span class="i18n-en"><%= String(advEn[ai] || advZh[ai]).trim() %></span>
              </span>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </section>

  <section class="products-section <%= (config && config.products_bg) ? 'has-bg' : '' %>" id="products"
    <% if (config && config.products_bg) { %>style="--pbg:url('<%= config.products_bg %>')"<% } %>>
    <style>
      .products-section.has-bg::before { background-image: var(--pbg, none); }
      .products-section.has-cat-bg::before { background-image: var(--cat-bg) !important; opacity: 0.22; }
  
    /* 回顶按钮 */
    .scroll-top {
      position:fixed; bottom:90px; right:24px; z-index:89;
      width:44px; height:44px; border-radius:50%; border:none; cursor:pointer;
      background:var(--main); color:#fff; font-size:20px; box-shadow:0 4px 14px rgba(0,0,0,.2);
      display:none; align-items:center; justify-content:center;
      transition: opacity .2s, background .2s;
    }
    .scroll-top.show { display:flex; }
    .scroll-top:hover { background:var(--blue); }
    /* 手机返回 */
    .mobile-back {
      display:none; position:fixed; top:12px; left:12px; z-index:110;
      width:40px; height:40px; border-radius:50%; border:none; cursor:pointer;
      background:rgba(10,47,95,.9); color:#fff; font-size:18px;
      align-items:center; justify-content:center; box-shadow:0 2px 10px rgba(0,0,0,.25);
    }
    @media (max-width:768px) {
      .mobile-back { display:flex; }
      .header-inner { padding-left:56px; }
    }
    /* 社交图标 */
    .social-bar {
      display:flex; flex-wrap:wrap; gap:14px; justify-content:center; margin-top:16px;
    }
    .social-item {
      display:flex; flex-direction:column; align-items:center; gap:6px;
      text-decoration:none; color:#94A3B8; font-size:12px; min-width:64px;
    }
    .social-item img {
      width:40px; height:40px; border-radius:50%; object-fit:cover;
      border:2px solid rgba(255,255,255,.2); background:#fff;
    }
    .social-item:hover { color:var(--accent); }
    .social-item:hover img { border-color:var(--accent); }

  </style>
    <h2 class="section-title"><span class="i18n-zh">热门产品</span><span class="i18n-en">Hot Products</span></h2>
    <p class="section-sub"><span class="i18n-zh">按分类筛选 · 支持多币种显示</span><span class="i18n-en">Filter by category · Multi-currency</span></p>
    <div class="products-layout">
      <aside class="sidebar-cat">
        <h4><span class="i18n-zh">分类</span><span class="i18n-en">Categories</span></h4>
        <div class="cat-list">
          <a href="/#products" class="cat-filter <%= (!catId || catId==='') ? 'active' : '' %>" data-cat=""><span class="i18n-zh">全部</span><span class="i18n-en">All</span></a>
          <% if (typeof categories !== 'undefined' && categories && categories.length) { %>
            <% categories.forEach(function(c){ %>
              <a href="/?cat=<%= c.id %><%= keyword ? '&q='+encodeURIComponent(keyword) : '' %>#products"
                 class="cat-filter <%= String(catId) === String(c.id) ? 'active' : '' %>"
                 data-cat="<%= c.id %>"
                 data-bg="<%= c.bg_image || '' %>">
                <span class="i18n-zh"><%= c.name_zh || c.name_en || ('分类'+c.id) %></span>
                <span class="i18n-en"><%= c.name_en || c.name_zh || ('Cat '+c.id) %></span>
              </a>
            <% }); %>
          <% } %>
        </div>
      </aside>
      <div>
        <form class="toolbar" method="get" action="/">
          <% if (catId) { %><input type="hidden" name="cat" value="<%= catId %>"><% } %>
          <input type="text" name="q" value="<%= keyword || '' %>" placeholder="OE / Name / Model" style="flex:1;min-width:160px;">
          <select name="sort">
            <option value="date" <%= sort==='date'?'selected':'' %>>Latest</option>
            <option value="priceAsc" <%= sort==='priceAsc'?'selected':'' %>>Price ↑</option>
            <option value="priceDesc" <%= sort==='priceDesc'?'selected':'' %>>Price ↓</option>
          </select>
          <button type="submit" class="btn btn-main" style="background:var(--blue);color:#fff;"><span class="i18n-zh">搜索</span><span class="i18n-en">Search</span></button>
        </form>
        <div class="product-grid">
          <% if (typeof products !== 'undefined' && products && products.length) { %>
            <% products.forEach(function(p){ 
              var img = p.image || (p.images ? String(p.images).split(',')[0] : '') || 'https://via.placeholder.com/400x400?text=No+Image';
            %>
              <a href="/product/<%= p.id %>" class="card">
                <div class="card-img"><img src="<%= img %>" alt="" loading="lazy" onerror="this.src='https://via.placeholder.com/400x400?text=No+Image'"></div>
                <div class="card-body">
                  <h3>
                    <span class="i18n-zh"><%= p.name_zh || p.name_en || '未命名' %></span>
                    <span class="i18n-en"><%= p.name_en || p.name_zh || 'Unnamed' %></span>
                  </h3>
                  <div class="card-meta">OE: <%= p.oe || '-' %> · <%= p.model || '' %></div>
                  <div class="price" data-hkd="<%= p.price || 0 %>">
                    <span class="cur-sym">HK$</span><span class="cur-val"><%= Number(p.price||0).toFixed(2) %></span>
                  </div>
                </div>
              </a>
            <% }); %>
          <% } else { %>
            <div class="empty" style="grid-column:1/-1;">
              <span class="i18n-zh">暂无产品，请先在后台导入数据包或添加产品。</span>
              <span class="i18n-en">No products yet. Import a datapack or add products in admin.</span>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </section>

  <section class="inquiry-section <%= (config && config.inquiry_bg) ? 'has-bg' : '' %>" id="inquiry"
    <% if (config && config.inquiry_bg) { %>style="background-image:none;"<% } %>>
    <% if (config && config.inquiry_bg) { %>
    <style>.inquiry-section.has-bg::before{background-image:url('<%= config.inquiry_bg %>');}</style>
    <% } %>
    <h2 class="section-title">
      <span class="i18n-zh"><%= (config && config.inquiry_title_zh) || '联系我们 / 询盘' %></span>
      <span class="i18n-en"><%= (config && config.inquiry_title_en) || 'Contact / Inquiry' %></span>
    </h2>
    <p class="section-sub">
      <span class="i18n-zh"><%= (config && config.inquiry_desc_zh) || '欢迎询价 · 快速回复 · 支持 OEM / ODM 与小批量试单' %></span>
      <span class="i18n-en"><%= (config && config.inquiry_desc_en) || 'Welcome to inquire · Fast response · OEM / ODM & small MOQ supported' %></span>
    </p>
    <div class="inquiry-cards">
      <div class="inquiry-card">
        <div class="ic">💬</div>
        <h4>WhatsApp</h4>
        <p><%= (config && config.wa) || '+85264960641' %></p>
      </div>
      <div class="inquiry-card">
        <div class="ic">✉️</div>
        <h4>Email</h4>
        <p><%= (config && config.email) || 'sales@example.com' %></p>
      </div>
      <div class="inquiry-card">
        <div class="ic">📍</div>
        <h4><span class="i18n-zh">地址</span><span class="i18n-en">Address</span></h4>
        <p><%= (config && config.address) || 'Hong Kong' %></p>
        <% if (config && config.work_hours) { %>
        <p style="margin-top:6px;font-size:12px;"><span class="i18n-zh">工作时间</span><span class="i18n-en">Hours</span>: <%= config.work_hours %></p>
        <% } %>
      </div>
    </div>
    <div class="inquiry-cta">
      <a href="https://wa.me/<%= ((config && config.wa) || '85264960641').replace(/[^0-9]/g,'') %>" class="btn btn-main" style="background:var(--blue);color:#fff;" target="_blank" rel="noopener">WhatsApp</a>
      <% if (config && config.email) { %>
      <a href="mailto:<%= config.email %>" class="btn" style="background:#fff;border:1.5px solid #1E6BD9;color:#1E6BD9;">Email</a>
      <% } %>
    </div>
  </section>

  <% } else if (page === 'detail' && product) { %>
  <div class="detail-wrap">
    <p style="font-size:13px;color:var(--muted);margin-bottom:16px;">
      <a href="/"><span class="i18n-zh">首页</span><span class="i18n-en">Home</span></a> /
      <a href="/#products"><span class="i18n-zh">产品</span><span class="i18n-en">Products</span></a> /
      <span class="i18n-zh"><%= product.name_zh || product.name_en || '' %></span>
      <span class="i18n-en"><%= product.name_en || product.name_zh || '' %></span>
    </p>
    <div class="detail-grid">
      <div class="detail-img">
        <% var dimg = product.image || (product.images ? String(product.images).split(',')[0] : '') || 'https://via.placeholder.com/600x600?text=No+Image'; %>
        <img src="<%= dimg %>" alt="" onerror="this.src='https://via.placeholder.com/600x600?text=No+Image'">
      </div>
      <div class="detail-info">
        <h1>
          <span class="i18n-zh"><%= product.name_zh || product.name_en || '产品详情' %></span>
          <span class="i18n-en"><%= product.name_en || product.name_zh || 'Product Detail' %></span>
        </h1>
        <div class="meta">
          <p><strong>OE：</strong><%= product.oe || '-' %></p>
          <p><strong><span class="i18n-zh">适用型号</span><span class="i18n-en">Model</span>：</strong><%= product.model || '-' %></p>
          <p><strong><span class="i18n-zh">库存</span><span class="i18n-en">Stock</span>：</strong><%= product.stock != null ? product.stock : '-' %></p>
        </div>
        <div class="price" data-hkd="<%= product.price || 0 %>" style="font-size:22px;margin:12px 0;">
          <span class="cur-sym">HK$</span><span class="cur-val"><%= Number(product.price||0).toFixed(2) %></span>
        </div>
        <ul class="spec-list">
          <li>
            <strong><span class="i18n-zh">描述</span><span class="i18n-en">Description</span>：</strong>
            <span class="i18n-zh"><%= product.description_zh || product.description_en || '' %></span>
            <span class="i18n-en"><%= product.description_en || product.description_zh || '' %></span>
          </li>
        </ul>
        <a href="https://wa.me/<%= ((config && config.wa) || '85264960641').replace(/[^0-9]/g,'') %>?text=<%= encodeURIComponent('Inquiry: '+(product.name_zh||product.name_en||'')+' OE:'+(product.oe||'')) %>" class="btn btn-main" style="background:var(--blue);color:#fff;" target="_blank" rel="noopener">WhatsApp</a>
        <a href="/" class="btn" style="margin-left:8px;background:#fff;border:1px solid #D1D5DB;color:var(--main);"><span class="i18n-zh">返回列表</span><span class="i18n-en">Back</span></a>
      </div>
    </div>
  </div>
  <% } else { %>
  <div class="empty" style="padding:80px 20px;">
    <p><span class="i18n-zh">页面不存在或产品已下架</span><span class="i18n-en">Page not found or product offline</span></p>
    <a href="/" class="btn btn-main" style="margin-top:16px;background:var(--blue);color:#fff;"><span class="i18n-zh">返回首页</span><span class="i18n-en">Home</span></a>
  </div>
  <% } %>

  <footer>
    <div class="foot-inner">
      <div class="foot-col">
        <h4><span class="i18n-zh">关于我们</span><span class="i18n-en">About Us</span></h4>
        <p class="foot-brand"><%= (config && config.company_name) || 'AutoParts B2B' %></p>
        <p><%= (config && config.address) || 'Hong Kong / Shenzhen' %></p>
        <p style="margin-top:10px;">
          <span class="i18n-zh"><%= (config && config.about_zh) || '专业汽车零部件供应商，严格品控，支持 OEM / ODM，服务全球客户。' %></span>
          <span class="i18n-en"><%= (config && config.about_en) || 'Professional auto parts supplier with strict QC. OEM / ODM for global customers.' %></span>
        </p>
      </div>
      <div class="foot-col">
        <h4><span class="i18n-zh">联系我们</span><span class="i18n-en">Contact Us</span></h4>
        <p>Email: <%= (config && config.email) || '' %></p>
        <p>WhatsApp: <%= (config && config.wa) || '' %></p>
        <% if (config && config.contact_phone) { %><p><span class="i18n-zh">电话</span><span class="i18n-en">Phone</span>: <%= config.contact_phone %></p><% } %>
        <% if (config && config.work_hours) { %><p><span class="i18n-zh">工作时间</span><span class="i18n-en">Hours</span>: <%= config.work_hours %></p><% } %>
      </div>
      <div class="foot-col">
        <h4><span class="i18n-zh">快速链接</span><span class="i18n-en">Quick Links</span></h4>
        <a href="/"><span class="i18n-zh">首页</span><span class="i18n-en">Home</span></a>
        <a href="/#products"><span class="i18n-zh">产品中心</span><span class="i18n-en">Products</span></a>
        <a href="/#factory"><span class="i18n-zh">工厂展示</span><span class="i18n-en">Factory</span></a>
        <a href="/#inquiry"><span class="i18n-zh">询盘联系</span><span class="i18n-en">Inquiry</span></a>
      </div>
    </div>
    <div class="foot-social-wrap">
      <div class="lab"><span class="i18n-zh">关注我们</span><span class="i18n-en">Follow Us</span></div>
      <div class="social-bar" id="socialBar">
        <% if (config && config.social1_img) { %>
        <a class="social-item" href="<%= config.social1_url || '#' %>" target="_blank" rel="noopener">
          <img src="<%= config.social1_img %>" alt="">
          <% if (config.social1_text) { %><span><%= config.social1_text %></span><% } %>
        </a>
        <% } %>
        <% if (config && config.social2_img) { %>
        <a class="social-item" href="<%= config.social2_url || '#' %>" target="_blank" rel="noopener">
          <img src="<%= config.social2_img %>" alt="">
          <% if (config.social2_text) { %><span><%= config.social2_text %></span><% } %>
        </a>
        <% } %>
        <% if (config && config.social3_img) { %>
        <a class="social-item" href="<%= config.social3_url || '#' %>" target="_blank" rel="noopener">
          <img src="<%= config.social3_img %>" alt="">
          <% if (config.social3_text) { %><span><%= config.social3_text %></span><% } %>
        </a>
        <% } %>
      </div>
    </div>
    <div class="copyright"><%= (config && config.copyright_text) || '© 2026 AutoParts B2B. All rights reserved.' %></div>
  </footer>

  
  <button type="button" class="scroll-top" id="scrollTopBtn" title="回顶部">↑</button>
  <% if (config && config.wa) { %>
  <a href="https://wa.me/<%= String(config.wa).replace(/[^0-9]/g,'') %>" class="wa-float" target="_blank" rel="noopener">💬 WhatsApp</a>
  <% } %>

  <script>
    window.__RATES = {
      HKD: 1,
      USD: parseFloat('<%= (config && config.rate_usd) || "0.128" %>') || 0.128,
      CNY: parseFloat('<%= (config && config.rate_cny) || "0.93" %>') || 0.93,
      KRW: parseFloat('<%= (config && config.rate_krw) || "175" %>') || 175,
      JPY: parseFloat('<%= (config && config.rate_jpy) || "19.5" %>') || 19.5
    };
    window.__SYMBOLS = { HKD:'HK$', USD:'US$', CNY:'¥', KRW:'₩', JPY:'¥' };

    // 中英文切换（真正切换页面文案）
    (function(){
      var btns = document.querySelectorAll('.lang-btn');
      var saved = localStorage.getItem('lang') || 'zh';
      function setLang(lang) {
        document.documentElement.setAttribute('lang', lang === 'en' ? 'en' : 'zh-CN');
        btns.forEach(function(b){ b.classList.toggle('active', b.getAttribute('data-lang') === lang); });
        localStorage.setItem('lang', lang);
      }
      setLang(saved);
      btns.forEach(function(btn){
        btn.addEventListener('click', function(){ setLang(btn.getAttribute('data-lang')); });
      });
    })();

    // 货币切换
    (function(){
      var sel = document.getElementById('currencySelect');
      if (!sel) return;
      var rates = window.__RATES, symbols = window.__SYMBOLS;
      var saved = localStorage.getItem('currency') || 'HKD';
      if (rates[saved]) sel.value = saved;
      function format(n, cur) {
        if (cur === 'KRW' || cur === 'JPY') return Math.round(n).toLocaleString();
        return Number(n).toFixed(2);
      }
      function apply(cur) {
        var rate = rates[cur] || 1, sym = symbols[cur] || cur;
        document.querySelectorAll('.price[data-hkd]').forEach(function(el){
          var hkd = parseFloat(el.getAttribute('data-hkd')) || 0;
          var converted = hkd * rate;
          var symEl = el.querySelector('.cur-sym'), valEl = el.querySelector('.cur-val');
          if (symEl) symEl.textContent = sym;
          if (valEl) valEl.textContent = format(converted, cur);
        });
        localStorage.setItem('currency', cur);
      }
      apply(sel.value);
      sel.addEventListener('change', function(){ apply(sel.value); });
    })();

    // 工厂轮播
    (function(){
      var slides = document.querySelectorAll('#factorySlider .slide');
      var dots = document.querySelectorAll('.slider-dots span');
      if (!slides.length) return;
      var idx = 0;
      var delay = <%= (config && config.carousel_delay) ? (parseInt(config.carousel_delay,10)||4500) : 4500 %>;
      if (delay < 1500) delay = 4500;
      function show(i) {
        slides.forEach(function(s,j){ s.classList.toggle('active', j===i); });
        dots.forEach(function(d,j){ d.classList.toggle('active', j===i); });
        idx = i;
      }
      dots.forEach(function(d){ d.addEventListener('click', function(){ show(+d.getAttribute('data-index')); }); });
      if (slides.length > 1) setInterval(function(){ show((idx+1)%slides.length); }, delay);
    })();

    // 分类切换：AJAX 不整页刷新、不跳顶
    (function(){
      var sec = document.getElementById('products');
      var grid = document.querySelector('.product-grid');
      if (!sec || !grid) return;

      function applyBg(url) {
        if (!url) {
          sec.classList.remove('has-cat-bg');
          return;
        }
        sec.style.setProperty('--cat-bg', 'url('+url+')');
        sec.classList.add('has-bg', 'has-cat-bg');
      }

      function renderProducts(list) {
        if (!list || !list.length) {
          grid.innerHTML = '<div class="empty" style="grid-column:1/-1;">暂无产品</div>';
          return;
        }
        var lang = document.documentElement.getAttribute('lang') || 'zh-CN';
        var isEn = lang === 'en';
        grid.innerHTML = list.map(function(p){
          var img = p.image || (p.images ? String(p.images).split(',')[0] : '') || 'https://via.placeholder.com/400x400?text=No+Image';
          var name = isEn ? (p.name_en || p.name_zh || '') : (p.name_zh || p.name_en || '');
          var price = Number(p.price || 0).toFixed(2);
          return '<a href="/product/'+p.id+'" class="card">' +
            '<div class="card-img"><img src="'+img+'" alt="" loading="lazy" onerror="this.src=\'https://via.placeholder.com/400x400?text=No+Image\'"></div>' +
            '<div class="card-body"><h3>'+name+'</h3>' +
            '<div class="card-meta">OE: '+(p.oe||'-')+' · '+(p.model||'')+'</div>' +
            '<div class="price" data-hkd="'+(p.price||0)+'"><span class="cur-sym">HK$</span><span class="cur-val">'+price+'</span></div>' +
            '</div></a>';
        }).join('');
        // 重新应用货币
        var sel = document.getElementById('currencySelect');
        if (sel) sel.dispatchEvent(new Event('change'));
      }

      document.querySelectorAll('.cat-filter').forEach(function(a){
        a.addEventListener('click', function(e){
          e.preventDefault();
          var cat = this.getAttribute('data-cat') || '';
          var bg = this.getAttribute('data-bg') || '';
          document.querySelectorAll('.cat-filter').forEach(function(x){ x.classList.remove('active'); });
          this.classList.add('active');
          applyBg(bg);
          // 保持当前滚动，不跳顶
          var keepY = window.scrollY;
          var q = new URLSearchParams(window.location.search);
          var keyword = q.get('q') || '';
          var sort = q.get('sort') || 'date';
          var url = '/api/products?cat=' + encodeURIComponent(cat) + '&q=' + encodeURIComponent(keyword) + '&sort=' + encodeURIComponent(sort);
          fetch(url).then(function(r){ return r.json(); }).then(function(j){
            if (j.success) renderProducts(j.products);
            // 更新地址栏但不跳转
            var newUrl = cat ? ('/?cat=' + cat + (keyword ? '&q='+encodeURIComponent(keyword) : '') + '#products') : ('/' + (keyword ? '?q='+encodeURIComponent(keyword) : '') + '#products');
            history.pushState(null, '', newUrl);
            window.scrollTo(0, keepY);
          }).catch(function(){
            // 失败则正常跳转
            window.location.href = this.href;
          }.bind(this));
        });
      });

      var active = document.querySelector('.cat-filter.active[data-bg]');
      if (active && active.getAttribute('data-bg')) applyBg(active.getAttribute('data-bg'));
    })();

    // 回顶按钮
    (function(){
      var btn = document.getElementById('scrollTopBtn');
      if (!btn) return;
      window.addEventListener('scroll', function(){
        if (window.scrollY > 400) btn.classList.add('show');
        else btn.classList.remove('show');
      });
      btn.addEventListener('click', function(){
        window.scrollTo({ top: 0, behavior: 'smooth' });
      });
    })();

    // 手机返回键
    (function(){
      var btn = document.getElementById('mobileBack');
      if (!btn) return;
      btn.addEventListener('click', function(){
        if (window.history.length > 1) history.back();
        else window.location.href = '/';
      });
    })();
  </script>
</body>
</html>
